# 🚀 Пошаговая инструкция: Деплой на Yandex Cloud

## Для новичков - максимально подробно!

---

## 📋 Что вам понадобится

1. ✅ Российская банковская карта (для оплаты)
2. ✅ Номер телефона
3. ✅ Email
4. ✅ 30-60 минут времени
5. ✅ Аккаунт GitLab (если хотите автодеплой)

---

## 🎯 Шаг 1: Регистрация в Yandex Cloud

### 1.1 Переходим на сайт

**Ссылка:** https://cloud.yandex.ru

**Нажимаем:** "Попробовать бесплатно" или "Консоль"

### 1.2 Авторизация

Войдите через:
- Яндекс ID (если есть)
- Или создайте новый аккаунт

### 1.3 Активация пробного периода

После входа увидите предложение:

**"Получите грант 4000 ₽ на 60 дней"**

Нужно будет:
1. ✅ Ввести данные карты (деньги НЕ спишутся)
2. ✅ Подтвердить телефон
3. ✅ Принять условия

> ⚠️ **Важно:** Яндекс дает 4000₽ бесплатно на 60 дней! Этого хватит на 2-3 месяца тестирования.

---

## 🎯 Шаг 2: Создание облака и каталога

После регистрации вы в консоли Yandex Cloud.

### 2.1 Проверьте облако

Сверху слева должно быть:
- **Облако:** `cloud-xxxxx` (создается автоматически)
- **Каталог:** `default` (создается автоматически)

Если нет - создайте:
1. Нажмите на имя облака → "Создать облако"
2. Имя: `ai-call-center-cloud`
3. Создать каталог: `production`

---

## 🎯 Шаг 3: Создание виртуальной машины (VM)

### 3.1 Открываем Compute Cloud

В боковом меню слева:
**"Все сервисы"** → **"Compute Cloud"** → **"Виртуальные машины"**

### 3.2 Создаем VM

Нажимаем **"Создать ВМ"**

**Базовые параметры:**
```
Имя: ai-call-center-vm
Зона доступности: ru-central1-a (любая)
```

**Выбор образа:**
```
Операционная система: Ubuntu 22.04 LTS
```

**Вычислительные ресурсы:**
```
Платформа: Intel Ice Lake
vCPU: 2
RAM: 4 GB
Уровень производительности: 100%
```

> 💰 **Стоимость:** ~600₽/месяц (покрывается грантом!)

**Сетевые настройки:**
```
Сеть: default (создастся автоматически)
Публичный IP: Автоматически
```

**Доступ:**
```
Логин: ubuntu
SSH-ключ: [Нажмите "Сгенерировать новую пару ключей"]
```

> ⚠️ **ВАЖНО:** Скачайте приватный ключ! Он понадобится для подключения.

**Нажимаем:** "Создать ВМ"

⏱️ Ждем 2-3 минуты...

### 3.3 Получаем IP адрес

После создания увидите:
```
Публичный IPv4: 51.250.XXX.XXX
```

**Скопируйте этот IP!** Он понадобится.

---

## 🎯 Шаг 4: Подключение к VM

### Вариант А: Через браузер (проще)

1. В списке VM нажмите на вашу машину
2. Справа вверху: **"Подключиться"** → **"SSH в браузере"**
3. Готово! Терминал открылся в браузере

### Вариант Б: Через SSH клиент

**Windows (PowerShell):**
```powershell
ssh -i путь\к\скачанному\ключу.pem ubuntu@51.250.XXX.XXX
```

**Linux/Mac:**
```bash
chmod 600 ~/Downloads/ключ.pem
ssh -i ~/Downloads/ключ.pem ubuntu@51.250.XXX.XXX
```

Если спросит "Are you sure?" - пишите `yes`

---

## 🎯 Шаг 5: Установка на VM

### 5.1 Скачиваем и запускаем установочный скрипт

В терминале VM выполните:

```bash
# Скачиваем проект
git clone https://github.com/GriZZli1975/phone.git /opt/ai-call-center
cd /opt/ai-call-center

# Делаем скрипт исполняемым
chmod +x yandex-cloud/setup.sh

# Запускаем установку
sudo bash yandex-cloud/setup.sh
```

⏱️ Установка займет 5-10 минут.

Скрипт установит:
- ✅ Docker
- ✅ Docker Compose
- ✅ Yandex Cloud CLI
- ✅ Создаст директории
- ✅ Настроит firewall

---

## 🎯 Шаг 6: Создание Container Registry

### 6.1 Создаем реестр для Docker образов

В консоли Yandex Cloud:
1. **"Все сервисы"** → **"Container Registry"**
2. **"Создать реестр"**
3. Имя: `ai-call-center-registry`
4. Нажать **"Создать"**

### 6.2 Получаем ID реестра

После создания скопируйте **ID реестра** (вида `crp1234567890abcdef`)

---

## 🎯 Шаг 7: Настройка переменных окружения

### 7.1 Редактируем .env файл

На VM выполните:

```bash
cd /opt/ai-call-center
nano .env
```

**Заполните файл:**

```bash
# PostgreSQL
POSTGRES_USER=callcenter
POSTGRES_PASSWORD=Создайте_Сложный_Пароль_123!
POSTGRES_DB=callcenter

# OpenAI (получите на https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-proj-ваш-ключ-здесь

# ElevenLabs (получите на https://elevenlabs.io/app/settings/api-keys)
ELEVENLABS_API_KEY=ваш-ключ-здесь
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM

# SIP Provider (Новофон - настроим позже)
SIP_PROVIDER_HOST=sip.novofon.ru
SIP_USERNAME=ваш-логин
SIP_PASSWORD=ваш-пароль
SIP_PHONE_NUMBER=+7495XXXXXXX

# JWT Secret (сгенерируйте случайную строку)
JWT_SECRET=Случайная_Строка_Минимум_32_Символа_123456

# Yandex Container Registry
YC_REGISTRY=cr.yandex/ваш-ID-реестра-сюда
```

**Сохранить:** Ctrl+O, Enter, Ctrl+X

---

## 🎯 Шаг 8: Получение API ключей

### 8.1 OpenAI API ключ

1. Зайдите на https://platform.openai.com/signup
2. Зарегистрируйтесь / войдите
3. **"API keys"** → **"Create new secret key"**
4. Скопируйте ключ (начинается с `sk-`)
5. Вставьте в `.env` → `OPENAI_API_KEY=`

> 💰 Нужно пополнить баланс на $5-10 для использования

### 8.2 ElevenLabs API ключ

1. Зайдите на https://elevenlabs.io
2. Зарегистрируйтесь / войдите
3. **Profile** → **API Settings**
4. Скопируйте API ключ
5. Вставьте в `.env` → `ELEVENLABS_API_KEY=`

> 💰 Есть бесплатный тариф (10,000 символов/месяц)

---

## 🎯 Шаг 9: Сборка и запуск

### 9.1 Собираем Docker образ

```bash
cd /opt/ai-call-center

# Логинимся в Container Registry
echo "YOUR_OAUTH_TOKEN" | docker login \
  --username oauth \
  --password-stdin \
  cr.yandex

# Собираем образ
docker build -t cr.yandex/ваш-ID-реестра/ai-call-center:latest .

# Загружаем в реестр
docker push cr.yandex/ваш-ID-реестра/ai-call-center:latest
```

### 9.2 Запускаем приложение

```bash
docker-compose -f yandex-cloud/docker-compose.yc.yml up -d
```

⏱️ Ждем 1-2 минуты...

### 9.3 Проверяем запуск

```bash
# Проверяем контейнеры
docker-compose -f yandex-cloud/docker-compose.yc.yml ps

# Проверяем логи
docker-compose -f yandex-cloud/docker-compose.yc.yml logs -f
```

Должны увидеть:
```
backend_1   | ✅ FreeSWITCH listener запущен
backend_1   | ✅ База данных инициализирована
backend_1   | INFO: Uvicorn running on http://0.0.0.0:8000
```

Нажмите Ctrl+C чтобы выйти из логов.

---

## 🎯 Шаг 10: Проверка работы

### 10.1 Открываем в браузере

```
http://51.250.XXX.XXX:8000
```

(Подставьте ваш IP)

Должны увидеть страницу входа! 🎉

**Логин:**
```
Email: admin@example.com
Пароль: changeme123
```

---

## 🎯 Шаг 11: Настройка GitLab CI/CD (опционально)

Если хотите автоматический деплой при пуше в GitLab:

### 11.1 Создаем сервисный аккаунт в Yandex Cloud

1. В консоли: **"Все сервисы"** → **"IAM"** → **"Сервисные аккаунты"**
2. **"Создать сервисный аккаунт"**
3. Имя: `gitlab-ci`
4. Роли: `container-registry.images.puller`, `compute.admin`
5. **"Создать"**

### 11.2 Создаем ключ для сервисного аккаунта

1. Откройте созданный аккаунт
2. **"Создать новый ключ"** → **"Создать авторизованный ключ"**
3. Скачайте JSON файл

### 11.3 Добавляем переменные в GitLab

В вашем репозитории GitLab:
1. **Settings** → **CI/CD** → **Variables** → **Expand**
2. Добавьте переменные:

```
YC_SERVICE_ACCOUNT_KEY = [содержимое скачанного JSON]
YC_CLOUD_ID = [ID вашего облака]
YC_FOLDER_ID = [ID вашего каталога]
YC_REGISTRY_ID = [ID Container Registry]
VM_IP = [IP адрес VM]
SSH_PRIVATE_KEY = [Приватный SSH ключ]
OPENAI_API_KEY = [Ваш OpenAI ключ]
ELEVENLABS_API_KEY = [Ваш ElevenLabs ключ]
```

### 11.4 Пушим в GitLab

```bash
git add .
git commit -m "Setup CI/CD for Yandex Cloud"
git push origin main
```

GitLab автоматически запустит сборку и деплой! 🚀

---

## 🎯 Шаг 12: Настройка Новофон (телефония)

### 12.1 Регистрация

1. Зайдите на https://novofon.com
2. **"Регистрация"**
3. Заполните данные компании
4. Подтвердите email

### 12.2 Покупка номера

1. В личном кабинете: **"Номера"** → **"Купить номер"**
2. Выберите регион (Москва, СПб и т.д.)
3. Выберите номер
4. Оплатите (~100-300₽/мес)

### 12.3 Настройка SIP trunk

1. **"Настройки"** → **"SIP"**
2. **"Создать SIP trunk"**
3. Параметры:
   ```
   IP/Домен: 51.250.XXX.XXX (IP вашей VM)
   Порт: 5060
   ```
4. Скопируйте логин и пароль

### 12.4 Обновите .env

```bash
nano /opt/ai-call-center/.env
```

Добавьте данные Новофона:
```
SIP_USERNAME=логин-из-новофона
SIP_PASSWORD=пароль-из-новофона
SIP_PHONE_NUMBER=+7495XXXXXXX
```

### 12.5 Перезапустите контейнеры

```bash
docker-compose -f yandex-cloud/docker-compose.yc.yml restart
```

---

## ✅ Готово! Что дальше?

### Проверьте работу:

1. **Frontend:** http://51.250.XXX.XXX:8000
2. **API Docs:** http://51.250.XXX.XXX:8000/docs
3. **Health Check:** http://51.250.XXX.XXX:8000/health

### Тестовый звонок:

Позвоните на ваш номер Новофон - AI должен ответить!

---

## 📊 Мониторинг

### Проверка логов:
```bash
cd /opt/ai-call-center
docker-compose -f yandex-cloud/docker-compose.yc.yml logs -f
```

### Перезапуск:
```bash
docker-compose -f yandex-cloud/docker-compose.yc.yml restart
```

### Остановка:
```bash
docker-compose -f yandex-cloud/docker-compose.yc.yml down
```

### Обновление кода:
```bash
cd /opt/ai-call-center
git pull origin main
docker-compose -f yandex-cloud/docker-compose.yc.yml up -d --build
```

---

## 🆘 Помощь и поддержка

### Проблемы с VM:
- Консоль Yandex Cloud → Compute Cloud → Ваша VM → Логи

### Проблемы с Docker:
```bash
docker ps  # Список контейнеров
docker logs <container_id>  # Логи контейнера
```

### Проблемы с API ключами:
- Проверьте баланс OpenAI: https://platform.openai.com/usage
- Проверьте лимиты ElevenLabs: https://elevenlabs.io/app/usage

---

## 💰 Стоимость

**Примерный расчет на месяц:**

| Сервис | Стоимость |
|--------|-----------|
| Yandex Cloud VM | 600₽ |
| Новофон (номер) | 200₽ |
| Новофон (звонки) | 100-500₽ |
| OpenAI | $10-30 (~1000₽) |
| ElevenLabs | $5-15 (~500₽) |
| **ИТОГО** | **~2400-3300₽/мес** |

> 💡 Первые 60 дней покрывает грант 4000₽!

---

**🎉 Поздравляю! Ваш AI Call Center запущен на Yandex Cloud!**


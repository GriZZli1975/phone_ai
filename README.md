# 🤖 AI Call Center на FreeSWITCH

Полнофункциональный AI колл-центр с маршрутизацией звонков, AI суфлером для операторов и AI консультантом.

## 🎯 Возможности

### 1. 🎭 AI Маршрутизация звонков
- Автоматический анализ запроса клиента через GPT-4
- Интеллектуальная переадресация на нужного оператора
- Голосовое меню (IVR) с распознаванием речи

### 2. 💡 AI Суфлер для операторов
- Real-time подсказки во время разговора
- Анализ тональности и эмоций клиента
- Рекомендации по ведению диалога
- История клиента и предыдущих обращений

### 3. 🤖 AI Консультант
- Полностью автоматические ответы на типовые вопросы
- Качественная генерация голоса через ElevenLabs
- Распознавание речи через OpenAI Whisper
- Бесшовная передача оператору при необходимости

### 4. 🖥️ Веб интерфейсы
- **Dashboard операторов**: real-time подсказки, история клиента
- **Админ панель**: управление маршрутизацией, настройки AI
- **Аналитика**: статистика звонков, эффективность AI
- **История звонков**: записи, транскрипты, поиск

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────────┐
│  VoIP-провайдер (Новофон)                           │
│  - Телефонные номера (+7)                           │
│  - Подключение к PSTN                               │
└──────────────────┬──────────────────────────────────┘
                   ↓ SIP Trunk
┌─────────────────────────────────────────────────────┐
│  Railway / VPS                                       │
│                                                      │
│  ┌──────────────────────────────────────────────┐  │
│  │ FreeSWITCH (Телефония)                       │  │
│  │ - Обработка звонков                          │  │
│  │ - Real-time audio streaming                  │  │
│  │ - IVR, маршрутизация                         │  │
│  └───────────────┬──────────────────────────────┘  │
│                  ↓ Event Socket Layer (ESL)         │
│  ┌──────────────────────────────────────────────┐  │
│  │ FastAPI Backend (Python)                     │  │
│  │ - AI логика (GPT-4)                          │  │
│  │ - STT (Whisper)                              │  │
│  │ - TTS (ElevenLabs)                           │  │
│  │ - WebSocket для суфлера                      │  │
│  │ - REST API                                    │  │
│  └───────────────┬──────────────────────────────┘  │
│                  ↓                                   │
│  ┌──────────────────────────────────────────────┐  │
│  │ PostgreSQL                                    │  │
│  │ - История звонков                            │  │
│  │ - Записи разговоров                          │  │
│  │ - Настройки маршрутизации                    │  │
│  └──────────────────────────────────────────────┘  │
│                  ↓                                   │
│  ┌──────────────────────────────────────────────┐  │
│  │ React Frontend                                │  │
│  │ - Dashboard операторов                       │  │
│  │ - Админ панель                               │  │
│  │ - Аналитика                                   │  │
│  └──────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

## 🚀 Технологический стек

### Backend
- **FreeSWITCH** - телефония (open-source)
- **Python 3.11** - основной язык
- **FastAPI** - веб фреймворк + WebSocket
- **ESL (Event Socket Layer)** - интеграция с FreeSWITCH
- **PostgreSQL** - база данных
- **Redis** - кэширование (опционально)

### AI Сервисы
- **OpenAI GPT-4** - анализ и генерация диалогов
- **OpenAI Whisper** - распознавание речи (STT)
- **ElevenLabs** - синтез речи (TTS)

### Frontend
- **React 18** - UI фреймворк
- **TailwindCSS** - стилизация
- **WebSocket** - real-time обновления
- **Recharts** - графики и аналитика

### DevOps
- **Docker** - контейнеризация
- **Docker Compose** - оркестрация
- **Railway** - деплой (или любой VPS)

## 📋 Требования

### VoIP-провайдер
Для работы нужен VoIP-провайдер с SIP trunk. Рекомендуемые:
- **Новофон (Novofon)** - от 100₽/мес (Россия)
- **Zadarma** - от $1/мес
- **VoIP.ms** - от $1/мес

### API ключи
- **OpenAI API key** - [platform.openai.com](https://platform.openai.com)
- **ElevenLabs API key** - [elevenlabs.io](https://elevenlabs.io)

## ⚙️ Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/your-username/ai-call-center.git
cd ai-call-center
```

### 2. Настройка переменных окружения

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
# OpenAI
OPENAI_API_KEY=sk-...

# ElevenLabs
ELEVENLABS_API_KEY=...

# Database
DATABASE_URL=postgresql://user:password@postgres:5432/callcenter

# FreeSWITCH ESL
FREESWITCH_HOST=freeswitch
FREESWITCH_PORT=8021
FREESWITCH_PASSWORD=ClueCon

# SIP провайдер (Новофон)
SIP_PROVIDER_HOST=sip.novofon.ru
SIP_USERNAME=ваш_логин
SIP_PASSWORD=ваш_пароль
SIP_PHONE_NUMBER=+7495XXXXXXX

# JWT для веб интерфейса
JWT_SECRET=your-secret-key-change-me
```

### 3. Запуск на Railway

#### Вариант А: Через Railway CLI

```bash
# Установите Railway CLI
npm install -g @railway/cli

# Войдите в аккаунт
railway login

# Создайте проект
railway init

# Добавьте переменные окружения
railway variables set OPENAI_API_KEY=sk-...
railway variables set ELEVENLABS_API_KEY=...
# ... остальные переменные

# Деплой
railway up
```

#### Вариант Б: Через GitHub

1. Создайте репозиторий на GitHub
2. Пушьте код: `git push origin main`
3. Зайдите на [railway.app](https://railway.app)
4. Создайте новый проект из GitHub репозитория
5. Добавьте переменные окружения в настройках
6. Railway автоматически задеплоит проект

### 4. Локальный запуск (для разработки)

```bash
# Запуск через Docker Compose
docker-compose up -d

# Логи
docker-compose logs -f

# Остановка
docker-compose down
```

Сервисы будут доступны:
- FreeSWITCH: `5060` (SIP), `8021` (ESL)
- Backend API: `http://localhost:8000`
- Frontend: `http://localhost:3000`
- PostgreSQL: `localhost:5432`

## 🔧 Настройка VoIP-провайдера

### Новофон (Novofon)

1. Зарегистрируйтесь на [novofon.com](https://novofon.com)
2. Купите номер (от 100₽/мес)
3. Перейдите в "Настройки" → "SIP"
4. Создайте SIP trunk:
   - Имя: `freeswitch`
   - IP: `ваш_railway_ip` (получите после деплоя)
   - Порт: `5060`
5. Скопируйте логин/пароль в `.env`

### Входящие звонки

В настройках номера укажите:
- **SIP URI**: `sip:incoming@ваш_railway_ip:5060`

### Исходящие звонки

Настроены автоматически через SIP trunk в FreeSWITCH.

## 📱 Использование

### Веб интерфейсы

После деплоя откройте:

**Dashboard оператора:**
```
https://ваш-проект.railway.app/operator
```

**Админ панель:**
```
https://ваш-проект.railway.app/admin
```

**Логин по умолчанию:**
- Email: `admin@example.com`
- Пароль: `changeme123`

⚠️ **Обязательно смените пароль после первого входа!**

### Тестовый звонок

1. Позвоните на ваш номер (+7 XXX XXX-XX-XX)
2. AI приветствует вас
3. Скажите ваш запрос (например "хочу сделать заказ")
4. AI маршрутизирует на нужного оператора или отвечает сам

### AI Суфлер

1. Оператор заходит в Dashboard
2. При входящем звонке видит:
   - Кто звонит
   - История клиента
   - Real-time транскрипция разговора
   - AI подсказки по ходу диалога

### Настройка маршрутизации

В админ панели:

1. "Настройки" → "Маршрутизация"
2. Добавьте правила:
   ```
   Ключевые слова: "продажи, купить, заказ"
   → Отдел продаж (ext. 101)
   
   Ключевые слова: "проблема, не работает, жалоба"
   → Тех. поддержка (ext. 102)
   
   По умолчанию:
   → AI Консультант
   ```

## 🎛️ API документация

После запуска доступна Swagger UI:

```
http://localhost:8000/docs
```

### Основные эндпоинты:

#### Звонки
```
GET  /api/calls              - История звонков
GET  /api/calls/{id}         - Детали звонка
POST /api/calls/{id}/record  - Получить запись
```

#### AI
```
POST /api/ai/analyze         - Анализ текста
POST /api/ai/route           - Определить маршрут
POST /api/ai/suggest         - Получить подсказку
```

#### WebSocket
```
WS /ws/supervisor/{call_id}  - Real-time суфлер
WS /ws/calls                 - Live обновления звонков
```

## 💰 Примерная стоимость

### Инфраструктура
| Сервис | Стоимость |
|--------|-----------|
| Railway (2GB RAM) | $10-20/мес |
| PostgreSQL (Railway) | $5/мес |
| Новофон (номер) | 100-300₽/мес |
| Входящие звонки | Бесплатно |
| Исходящие звонки | 0.5₽/мин |

### AI Сервисы
| Сервис | Стоимость | На 1000 минут |
|--------|-----------|---------------|
| OpenAI GPT-4 | $2.50/1M токенов | ~$10 |
| OpenAI Whisper | $0.006/мин | $6 |
| ElevenLabs | $5-30/мес | ~$15 |

**Итого:** ~$50-70/мес для 1000 минут звонков

## 🔒 Безопасность

### Включено по умолчанию:
- ✅ JWT авторизация для веб интерфейса
- ✅ Rate limiting на API
- ✅ CORS защита
- ✅ Валидация всех входных данных
- ✅ Шифрование паролей (bcrypt)

### Рекомендации:
1. Смените JWT_SECRET
2. Включите HTTPS (Railway делает автоматически)
3. Настройте firewall для FreeSWITCH
4. Регулярно обновляйте зависимости
5. Используйте сильные пароли для SIP

## 📊 Мониторинг

Встроенные метрики:

```
GET /api/metrics
```

Показывает:
- Количество активных звонков
- Загрузка операторов
- Процент обработки AI
- Средняя длительность звонка
- Качество распознавания

## 🐛 Troubleshooting

### FreeSWITCH не запускается
```bash
docker-compose logs freeswitch
```

### AI не отвечает
Проверьте API ключи:
```bash
railway variables
```

### Нет аудио во время звонка
Проблема с NAT/RTP. Проверьте `freeswitch/config/vars.xml`:
```xml
<X-PRE-PROCESS cmd="set" data="external_sip_ip=auto-nat"/>
<X-PRE-PROCESS cmd="set" data="external_rtp_ip=auto-nat"/>
```

### Входящие звонки не приходят
1. Проверьте настройки в Новофоне
2. Убедитесь что порт 5060 открыт
3. Проверьте логи: `docker-compose logs freeswitch`

## 🤝 Поддержка

- 📧 Email: support@example.com
- 💬 Telegram: @your_support
- 🐛 Issues: [GitHub Issues](https://github.com/your-username/ai-call-center/issues)

## 📝 Лицензия

MIT License - используйте как хотите!

## 🙏 Благодарности

- FreeSWITCH - за отличную телефонию
- OpenAI - за мощные AI модели
- ElevenLabs - за качественный TTS

---

**Сделано с ❤️ для автоматизации колл-центров**

